{
  "functions": {
    "api/s/[token].ts": {
      "memory": 256,
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/s/:token", "destination": "/api/s/:token" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
