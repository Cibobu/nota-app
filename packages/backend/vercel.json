{
  "version": 2,
  "installCommand": "npm install --install-strategy=nested",
  "buildCommand": "cd ../.. && npm run build -w packages/frontend && cd packages/backend && npx prisma generate && npm run build",
  "functions": {
    "api/index.ts": {
      "memory": 256,
      "maxDuration": 10
    }
  },
  "routes": [{ "src": "/(.*)", "dest": "api/index.ts" }]
}
